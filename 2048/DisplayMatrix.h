#pragma once

#include <iostream>
#include <Windows.h>
#include <string>
#include "Matrix.h"

using std::cout, std::endl, std::string;

void display(const Matrix &);
void hline(const int &);
string helper(const string &);